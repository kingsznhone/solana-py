[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "solana_kit"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
python-source = "."
module-name = "solana_kit._core"
